#!/usr/bin/make -f
# See debhelper(7) (uncomment to enable)
# output every command that modifies files on the build system.
#export DH_VERBOSE = 1



%:
	dh $@

override_dh_auto_build:
	make all

override_dh_fixperms:
	dh_fixperms
	chmod 4755 debian/slimbookface/usr/bin/slimbookface-helper
